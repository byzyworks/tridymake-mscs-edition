{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/src/app.js",
            "cwd": "${workspaceFolder}",
            "args": ["console", "--log-level", "debug"],
            "outputCapture": "std",
            "console": "externalTerminal"
        }
    ]
}