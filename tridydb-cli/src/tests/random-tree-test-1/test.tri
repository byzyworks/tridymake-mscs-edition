@tridy;

@new random-tree;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in (random-tree ?) | (random-tree >>> ?) @new random-node;

@in random-tree | (random-tree >>> *) @new child-of-all;

@get random-tree;
