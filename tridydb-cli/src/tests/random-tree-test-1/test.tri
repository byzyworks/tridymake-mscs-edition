@tridy;

@new @none @as random-tree;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in (random-tree ?) | (random-tree >>> ?) @new @none @as random-node;

@in random-tree | (random-tree >>> *) @new @none @as child-of-all;

@get random-tree;
