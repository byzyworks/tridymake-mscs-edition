@tridy;

@new @as random-tree;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in (random-tree ?) | (random-tree // ?) @new @as random-node;

@in random-tree | (random-tree // *) @new @as child-of-all;

@get random-tree;
